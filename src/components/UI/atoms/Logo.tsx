import styled from "styled-components";

const Title = styled.h1`
  font-size: 1.3rem;
  color: #282c81;
  margin-block: 0;
  text-decoration: underline;
`;

const Logo = () => {
  return <Title>Skyship</Title>;
};

export default Logo;
